V2 Work Log — Unwrapped ("The Depth")
Deadline: Tuesday, May 5, 2026

Feedback incorporated from V1 review:
- Deploy to Vercel (public URL) so production bugs surface rather than hiding behind localhost tunneling
- Ensure the frontend is visibly live and demo-able at a stable public URL
- Auth flows need to be tested in a real production environment, not just localhost

=== FINALIZED V2 FEATURE PLAN ===

1. VERCEL DEPLOYMENT
   - Install Vercel CLI, link repo, push env vars to Vercel
   - Add production URL to Spotify developer dashboard as redirect URI
   - Verify full auth + dashboard flow in production

2. LLM INTEGRATION (Ollama local / Groq production)
   - Vercel AI SDK with @ai-sdk/openai-compatible for provider abstraction
   - Local: Ollama (llama3.2 or similar, free, no key)
   - Production: Groq free tier (Llama 3.3 70B, API key only, no CC)
   - Same code, different env vars per environment

3. LISTENER ARCHETYPE (dashboard feature)
   - Named archetype category assigned by rule from bubble score components
     (e.g. "The Archaeologist", "The Passenger", "The Retreater", "The Omnivore")
   - Bespoke prose paragraph from LLM using user's actual stats
   - Appears on dashboard below Filter Bubble Score hero
   - Cached in Supabase per user, regenerated weekly

4. TEMPORAL BUBBLE SCORE
   - Store bubble_score + timestamp in user_visits table on each dashboard load
   - Line chart showing score history across visits
   - AI-generated 1-2 sentence trend narrative
   - First-visit prompt: "Come back next week to see your trend"

5. MUSIC HISTORY TIMELINE (/history page)
   - Separate page linked from dashboard nav
   - Pulls user's top 3 genres via existing Last.fm enrichment layer
   - Per-genre card: origin era + geography, 3-5 pioneering figures with images
     (Wikimedia Commons - personal/non-commercial use)
   - LLM generates narrative prose per genre, cached in Supabase by genre name
   - Visual lineage: HORIZONTAL RAIL (SVG/D3) reading left-to-right as a story
     "New Orleans 1910 → Miles Davis 1955 → D'Angelo 2000 → your listening today"
   - Interactive timeline scrubber showing when user's genres emerged

=== PROGRESS LOG ===
- Deployed to Vercel at https://unwrapped-one.vercel.app
- Added all env vars (Production + Preview) via Vercel CLI
- Added production redirect URI to Spotify developer dashboard
- Installed ai@6.0.175, @ai-sdk/groq@3.0.38, @ai-sdk/openai@3.0.60
- Created lib/ai/client.ts: getModel() returns Groq (llama-3.3-70b-versatile)
  in production or Ollama (llama3.2 via OpenAI-compat API) in local dev
- Created lib/ai/archetype.ts: rule-based archetype assignment (6 types) +
  LLM prose generation + Supabase cache with 7-day TTL
- Created supabase/migrations/003_user_archetypes.sql (needs manual apply)
- Added archetype card to dashboard between toggle and score hero
- Archetype card shows named type (accent color) + bespoke prose paragraph
- Created lib/analysis/visit-tracking.ts: recordVisit (once/day dedup),
  getVisitHistory, generateTrendNarrative (LLM, 1-2 sentences)
- Created BubbleScoreTrendChart: Recharts LineChart with tier reference lines
- Dashboard records visit + fetches history on every load (real users only)
- Shows trend chart + AI narrative when >= 2 visits; first-visit prompt otherwise
- Created lib/ai/genre-history.ts: generateText + Output.object (AI SDK v6
  structured output), Wikimedia Commons image fetch, Supabase cache (30-day TTL)
- Created supabase/migrations/004_genre_history_cache.sql (needs manual apply)
- Created components/visualizations/lineage-rail.tsx: client-side SVG horizontal
  rail with alternating labels, tier dots, accent endpoints
- Created app/history/page.tsx: /history route with per-genre cards, pioneer
  grid with Wikipedia images, lineage rail, AI-generated cultural context
- Added "Music history" nav link in dashboard header

=== PRODUCTION DEBUGGING ===
- Diagnosed /history page returning empty: root cause was twofold:
  1. Output.object (AI SDK v6 structured output) uses json_schema mode which
     llama-3.3-70b-versatile on Groq does not support — switched to plain
     generateText + JSON.parse
  2. Zod schema had strict .max() char limits that the model's real responses
     exceeded — removed all max() constraints, kept structural validation only
- Verified via curl that /history now renders Jazz and indie folk genre cards
- Removed temporary debug API route (/api/debug/genre-test)
- UI overhaul: removed all v1/v2/class project references from homepage,
  dashboard, setup page, and nav — app now reads as a real product

=== V2 REVIEW POLISH PASS — MAY 4, 2026 ===
- Fixed dashboard honesty-note copy so the production UI no longer says macro
  data "will be replaced" later; now frames preloaded macro trends as
  directional context and points to the listed source set.
- Clarified listener archetype scope: archetype remains based on the 6-month
  listening profile, matching the current weekly Supabase cache model, and the
  dashboard now says this explicitly.
- Renamed the short-term dashboard tab from "4 weeks" to "Recent" while keeping
  the Spotify time range implementation unchanged internally.
- Added plain-English sub-score context in the dashboard breakdown:
  genre concentration, artist repetition, popularity skew, and temporal
  consistency now each explain what a high number means.
- Cleaned dead /history code: removed the unused getTopGenres helper,
  unused topArtistNames variable, and unused Last.fm enrichment import.
- Restyled the /history "Back to dashboard" link as an obvious nav control
  instead of using eyebrow label styling.
- Replaced the /history pioneer portrait fallback emoji with an accent-colored
  monogram to better match the app's visual language.
- Made the global layout auth-aware by reading only the existing Spotify
  session cookies: authenticated users now see a logout control instead of
  "Log in with Spotify" in the header, without loading the full profile in
  app/layout.tsx.
- Added route-level skeleton loading states for /dashboard and /history so
  cold server work, cache misses, and LLM generation no longer feel like a
  blank page.
- Changed genre history loading to report partial failures: if one of the
  user's top genres fails to generate, the page shows a small notice naming the
  failed genre(s) instead of silently rendering fewer cards.
- Added a dashboard "Copy link" share action alongside "Share your score";
  it copies the absolute public share URL and shows copied/error feedback.
- Refactored dashboard range switching for speed:
  - app/dashboard/page.tsx now computes all three bubble scores once.
  - The medium-term score is reused for visit tracking and archetype generation
    to avoid duplicate server-side score computation.
  - Archetype lookup/generation and visit recording run in parallel after the
    profile loads.
  - DashboardContent now switches Recent / 6 months / All time in client state
    and updates the URL with history.replaceState, avoiding a full server
    round-trip and preserving scroll position.
- Added an optional precomputed medium-term score argument to
  getOrGenerateArchetype so uncached archetype generation can reuse work.
- Verified the polish/refactor pass with npm run lint, npm run type-check, and
  npm run build.
- Improved lineage rail label handling:
  - Labels and notes now derive their truncation budget from the actual spacing
    between SVG nodes instead of fixed global character limits.
  - First and last node labels are anchored inward so edge labels get usable
    space instead of clipping off the rail.
  - Full lineage event text is preserved in an SVG title for hover/tooltips.
- Re-verified after the lineage rail fix with npm run lint, npm run type-check,
  and npm run build.

=== BUILD ORDER ===
1. Vercel deployment (unblocks production testing)
2. LLM setup (Groq account + AI SDK integration)
3. Listener archetype (dashboard addition, self-contained)
4. Temporal bubble score (new DB table + chart + narrative)
5. Music history timeline (new page, most complex)
